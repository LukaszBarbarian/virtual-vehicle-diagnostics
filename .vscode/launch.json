{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Simulation",
            "type": "debugpy",
            "request": "launch",
            "module": "simulator.main",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
